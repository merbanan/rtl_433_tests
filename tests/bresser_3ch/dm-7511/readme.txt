Renkforce DM-7511 that uses a frequency of 433.98 MHz, bought at Conrad Electronics.

It looks like the data bits run into the following preamble without enough gap to detect the end of packet producing a spurious 42nd pulse.

