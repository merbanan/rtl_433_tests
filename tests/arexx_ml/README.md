# Arexx Multilogger IP-HA90, IP-TH78EXT, TSN-70E

See https://github.com/merbanan/rtl_433/pull/3488 for the decoder update
that added support for TSN-70E units transmitting an extended, 24-bit ID
(message length 7) instead of the usual 16-bit one (message length 5).
Unmerged as of this writing, author inonoob.

No raw `.cu8` captures were available, only the hex codes posted in the
PR discussion (see `codes_test.txt`).
